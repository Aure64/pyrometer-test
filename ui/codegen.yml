schema: ../backend/src/schema.graphql
documents: ./api.graphql
generates:
  ./src/api.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    # config:
    #   withHooks: false
